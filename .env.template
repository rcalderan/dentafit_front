# Copy this file to .env and fill in the values before running `npm run build:prod`
# .env is gitignored — never commit secrets to the repository.

# Base URL of the ECS backend service (no trailing slash)
API_BASE_URL=https://api.meuapp.com

# S3 bucket name — same secret used by the deploy step
AWS_S3_BUCKET=your-bucket-name
